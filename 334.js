// Напишите код, который сравнит две
// приведенные ниже даты и выведет сообщение о том,
// какая из них больше:

let date1 = "09-21";
let date2 = "09-23";

if (date1 > date2) {
  console.log(`${date1} больше, чем ${date2}`);
} else console.log(`${date2} больше, чем ${date1}`);
