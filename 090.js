// №1⊗jsPmSMMA
// Дан следующий объект:
let obj = { x: 1, y: 2, z: 3 };
// Дана переменная key, в которой хранится один из
// ключей нашего объекта. Выведите с помощью этой
// переменной соответствующий элемент объекта.

let key = "x";

console.log(obj[key]);

key = "z";
console.log(obj[key]);
