// undefined - неопределённость.
// такое значение будет если переменную объявили, но
// не записали в неё значение

let a;
console.log(a); // undefined

// Объявите переменную, не задавая ей значения.
// Выведите значение созданной переменной на экран.

let b;
console.log(b);
