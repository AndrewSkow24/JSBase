// Дан следующий массив:

let arr = [
  [1, 2, 3, [4, 5, [6, 7]]], // 0
  [8, [9, 10]], // 1
];
// Вручную, без цикла, найдите сумму элементов этого массива.

let sum =
  arr[0][0] +
  arr[0][1] +
  arr[0][2] +
  arr[0][3][0] +
  arr[0][3][1] +
  arr[0][3][2][0] +
  arr[0][3][2][1] +
  arr[1][0] +
  arr[1][1][0] +
  arr[1][1][1];
console.log(sum);

// проверка
let sum2 = 0;
for (let i = 1; i <= 10; i++) {
  sum2 += i;
}
console.log(sum2);
